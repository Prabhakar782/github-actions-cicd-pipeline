#apiVersion: v1
#kind: Service
#metadata:
#  name: spring-service
#spec:
#  type: NodePort
#  selector:
#    app: spring-app
#  ports:
#    - port: 80        # Service port (what external clients hit)
#      targetPort: 8181 # Pod container port
#      nodePort: 31300  # NodePort to access externally